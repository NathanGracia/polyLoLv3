version: '3.8'

services:
  polymarket-web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    volumes:
      - ./bets.db:/app/bets.db
    networks:
      - polymarket

networks:
  polymarket:
    driver: bridge
